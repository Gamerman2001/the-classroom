//will show data for a desk
import React, {useState}from 'react'

export default function Desk(props) {
  const {user, dailyData, Desk ,roomId, pos} = props

  return (
    <div>
      {/* {user}
      {dailyData} */}
    </div>
  )
}
